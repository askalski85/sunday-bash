#!/bin/bash
DAYS=${1:-5}
FUTURE_DAYS=${2:-${DAYS}}
CALENDAR=${DEFAULT_CALENDAR:-"undefined"}
STAMP=$(date -d "-${DAYS} days")
FUTURE_STAMP=$(date -d "+${FUTURE_DAYS} days")

gcalcli --calendar $CALENDAR agenda "$STAMP" "$FUTURE_STAMP"
